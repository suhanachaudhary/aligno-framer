
const TechIcon = ({ icon: Icon }) => {
    return (
        <div className="text-4xl text-white hover:text-blue-400 transition-all duration-300">
            <Icon />
        </div>
    );
};

export default TechIcon;
